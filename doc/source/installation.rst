============
Installation
============

At the command line::

    $ pip install zone_migration

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv zone_migration
    $ pip install zone_migration
